export const environment = {
  production: false,
  // Replace this with your GroqCloud API key.
  // Keep browser keys restricted to trusted domains for real deployments.
  groqApiKey: 'YOUR_GROQ_API_KEY',
};
